first, second, third = 'Doug', 22, 1984


def functionsReturningTuple(a, b, c):
    x = (c, a, b)
    return x


print(functionsReturningTuple(first, second, third))
print(functionsReturningTuple(first, second, third))